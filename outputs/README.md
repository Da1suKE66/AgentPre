# Runtime outputs

The checked-in directory is intentionally empty. On `lsh-stable32314`, runs are written under
`/cache/liluchen/agentpre/outputs` so that trajectories, rollouts, logs, and simulator data never
fill the small root filesystem.

